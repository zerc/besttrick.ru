<td class="notice_video__readed
    <% if (checkin.get('approved') === 0) { %> new">новое<% } %>
    <% if (checkin.get('approved') === 1) { %> approved">одобрено<% } %>
    <% if (checkin.get('approved') === 2) { %> not_approved">не одобрено<% } %>
</td>
<td><a href="<%= checkin.get('video_url') %>" target="_blank">видео</a></td>
<td><%= checkin.get('cones') %></td>
<td>
    <a href="#!profile-<%= checkin.get('user') %>" target="_blank"><%= checkin.get('username') %></a>
</td>
<td>
    <a href="#!trick<%= checkin.get('trick') %>" target="_blank"><%= checkin.get('trick_title') %></a>
</td>
<td style="text-align:right;">
    <a href="#" class="notice_ok">Ok</a>
    <a href="#" class="notice_bad">Bad</a>
</td>

